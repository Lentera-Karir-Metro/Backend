console.log('Deprecated: Mailtrap API test script has been removed.');
console.log('Use generic SMTP variables (SMTP_*) or Gmail App Password (EMAIL_USER/EMAIL_PASS). No HTTP provider is used now.');
process.exit(0);
